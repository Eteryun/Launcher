import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  gap: 0.4rem;

  > svg {
    color: #fff;
    width: 1.6rem;
    height: auto;
  }

  > input {
    width: 9.7rem;
    text-align: center;
  }
`;
